# Docker Compose configuration for Project Niyati
# Provides Neo4j service for local development
# SQLite is used for relational data (file-based, no container needed)

version: '3.8'

services:
  neo4j:
    image: neo4j:5.15-community
    container_name: niyati-neo4j
    environment:
      NEO4J_AUTH: neo4j/niyati_password
      NEO4J_PLUGINS: '["apoc", "graph-data-science"]'
      NEO4J_dbms_security_procedures_unrestricted: apoc.*,gds.*
      NEO4J_dbms_memory_heap_max__size: 2G
    ports:
      - "7474:7474"  # HTTP
      - "7687:7687"  # Bolt
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    healthcheck:
      test: ["CMD-SHELL", "cypher-shell -u neo4j -p niyati_password 'RETURN 1'"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  neo4j_data:
  neo4j_logs:
